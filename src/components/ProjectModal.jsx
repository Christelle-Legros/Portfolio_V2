import React from "react";

const ProjectModal = ({ description }) => {
  return (
    <div>
      <p>{description}</p>
      <button>Découvrir le projet</button>
    </div>
  );
};

export default ProjectModal;
